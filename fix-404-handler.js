/**
 * תיקון קריטי לנתיב התמלול
 * =======================
 *
 * יש בעיה בקובץ server.js - מטפל שגיאות 404 נמצא במקום הלא נכון.
 * 
 * הבעיה:
 * ------
 * 1. מטפל ה-404 נמצא בשורה ~1832 
 * 2. נתיב ה-transcribe נמצא בשורה ~1935
 * 3. האפליקציית Express מעבדת נתיבים לפי הסדר שהם מוגדרים
 * 4. לכן, כל בקשה ל-/transcribe נתפסת ע"י מטפל ה-404 ולא מגיעה לקוד התמלול
 * 
 * הפתרון:
 * -------
 * 1. פתח את הקובץ server.js
 * 2. מצא את הקוד הבא (בערך בשורה 1832):
 * 
 * ```javascript
 * // Custom 404 handler
 * app.use((req, res) => {
 *     console.log(`[404] No handler found for ${req.method} ${req.url}`);
 *     res.status(404).send(`
 *         <html>
 *             <head>
 *                 <title>404 - Not Found</title>
 *                 ...
 *             </head>
 *             ...
 *         </html>
 *     `);
 * });
 * ```
 * 
 * 3. חתוך (cut) את כל הבלוק הזה (מההערה // Custom 404 handler עד סגירת הסוגריים });)
 * 4. גלול לסוף הקובץ (אחרי כל קוד אחר) והדבק את הבלוק שחתכת שם
 * 
 * קוד לדוגמה לסדר הנכון:
 * -----------------------
 * 
 * ```javascript
 * // 1. מידלוור גלובלי - בתחילת הקובץ
 * app.use(cors());
 * app.use(express.json());
 * 
 * // 2. נתיבים ספציפיים - באמצע
 * app.get('/', (req, res) => { /* ... */ });
 * app.get('/proxy', (req, res) => { /* ... */ });
 * app.get('/transcribe', (req, res) => { /* ... */ });
 * 
 * // 3. מטפל שגיאות 404 - חייב להיות האחרון
 * app.use((req, res) => {
 *     res.status(404).send('Not Found');
 * });
 * ```
 * 
 * אחרי התיקון, נתיב ה-transcribe יעבוד כי הבקשה תגיע אליו לפני מטפל ה-404.
 */ 